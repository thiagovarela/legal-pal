services:
  postgres:
    image: postgres:15-alpine
    ports:
      - 5432:5432    
    environment:
      - POSTGRES_DB=gabo
      - POSTGRES_PASSWORD=gabo
      - POSTGRES_USER=gabo    
      - DEBUG="true"  